{
  "name": "ascii-diag",
  "version": "0.0.1",
  "author": "Dan Persa",
  "license": "MIT",
  "scripts": {
    "test": "npm run test-renderer && npm run test-editor",
    "test-renderer": "cd packages/ascii-diag-renderer && yarn test",
    "test-editor": "cd packages/ascii-diag-editor && yarn test",
    "type-check": "tsc --noEmit",
    "type-check:watch": "tsc --noEmit --watch"
  },
  "devDependencies": {
    "@types/jest": "^26.0.15",
    "gh-pages": "^1.2.0",
    "jest": "^26.6.3",
    "jest-canvas-mock": "^2.3.0",
    "parcel-bundler": "^1.3.1",
    "ts-jest": "^26.4.4",
    "typescript": "^4.0.5"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/danpersa/ascii-diag.git"
  },
  "jest": {
    "setupFiles": [
      "jest-canvas-mock"
    ]
  }
}
